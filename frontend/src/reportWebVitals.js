const reportWebVitals = () => {
  // This is a minimal implementation that can be expanded later if analytics are needed
  if (process.env.NODE_ENV !== 'production') {
    console.log('Web Vitals reporting is disabled in production');
  }
};

export default reportWebVitals; 